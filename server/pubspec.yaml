name: outlook_auth_server
description: Stateless HTTP relay that verifies email credentials via real SMTP AUTH.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  enough_mail: ^2.1.6
